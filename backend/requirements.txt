# Core
Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2
drf-spectacular==0.27.2

# Database
psycopg2-binary==2.9.9

# Config
python-dotenv==1.0.1
dj-database-url==2.1.0

# Forecasting / analytics
# numpy is required by the forecasting engine; statsmodels is optional (the engine
# degrades gracefully to a NumPy least-squares fit when it is unavailable).
# pandas is pulled in transitively by statsmodels. scikit-learn was unused and removed.
numpy==1.26.4
statsmodels==0.14.2

# Reporting
reportlab==4.2.0

# Production server
gunicorn==22.0.0
whitenoise==6.6.0

# Testing
pytest==8.2.0
pytest-django==4.8.0
factory-boy==3.3.0
