"""Forecasting is computed on demand from apps.stock.MedicineUsage; no
persistent models are required. This module is intentionally minimal so the app
participates in migrations without owning tables."""
